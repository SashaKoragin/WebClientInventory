{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Echo vars",
            "command": "echo",
            "args": [
                "${env:USERNAME}",
                "workspaceFolder = ${workspaceFolder}\\Client",
                "workspaceFolder = ${workspaceRoot}\\Client"
            ],
            "type": "shell"
        },
        {
            "type": "npm",
            "script": "start",
            "problemMatcher": [],
        }
    ]
}